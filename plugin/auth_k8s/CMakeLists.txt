FIND_PACKAGE(CURL)

IF(NOT CURL_FOUND)
  RETURN()
ENDIF()

SET_PACKAGE_PROPERTIES(CURL PROPERTIES TYPE REQUIRED)
INCLUDE_DIRECTORIES(${CURL_INCLUDE_DIRS})

MYSQL_ADD_PLUGIN(auth_k8s
  auth_k8s.c
  tokenreview_api.c
  LINK_LIBRARIES ${CURL_LIBRARIES}
  COMPONENT auth-k8s
  MODULE_ONLY)
